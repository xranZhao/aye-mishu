# 未尽

个人 AI 秘书 PWA。数据、周报和 DeepSeek API Key 只保存在当前浏览器的 localStorage。

## 本地运行

在本目录运行：

```powershell
python -m http.server 8080
```

打开 `http://localhost:8080`。

## GitHub Pages

推送仓库后，在 GitHub 仓库的 Settings -> Pages 中选择 `main` 分支根目录。

不要把 DeepSeek Key 写进任何源码文件。首次在 App 的“设置”页填写即可。
